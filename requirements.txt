# Environment for STSF-TPLS.
# Pinned to the versions the reported evaluation numbers were computed in
# (torch 2.3.0+cu118, local RTX 4090). Training ran on torch 2.7.0+cu128
# (remote RTX 4080 SUPER), cross-checked consistent. The determinism claims
# are torch-version-specific; relax the pins to ranges for a newer stack.
#
# torch / torchvision are CUDA builds -- install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu118
torch==2.3.0+cu118
torchvision==0.18.0+cu118
numpy==1.26.4
scipy==1.15.3
opencv-python==4.11.0.86
pillow==11.3.0
scikit-learn==1.7.2
matplotlib==3.10.7
tqdm==4.67.1
pyyaml==6.0.3
tensorboard==2.20.0
kornia==0.8.2
pytest==9.0.1      # for the test/ anchor suite (metric / Hadamard / WBC-map / param-count guards)
